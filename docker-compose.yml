version: "2.4"
volumes:
  gapapp:
    external: true
  static:
services:
  gapimage:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    image: gapimage:latest
    container_name: gapapp
    environment:
      - TZ=Europe/Rome
      - MODULE_NAME=gap.wsgi
      - VARIABLE_NAME=application
    volumes:
      - gapapp:/app
      - static:/app/static
    ports:
      - "8000:80"
    restart: unless-stopped
